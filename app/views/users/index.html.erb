<h1>All Users</h1>

<% @users.each do |user| %> 
	<ul class="users">
		<li>
			<h3>
				<%= gravatar_for user, size: 50 %>
				<%= link_to user.username, user %>
			</h3>
		</li>
	</ul>
<% end %> 

<%= will_paginate @users %>